apiVersion: 1

providers:
  - name: 'Sidra Infrastructure'
    orgId: 1
    folder: 'Sidra'
    folderUid: 'sidra'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
